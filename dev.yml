services:
  liv-book-reader:
    environment:
      PORT: ${PORT:-3000}
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL:-http://10.0.0.193:3000}
      SESSION_SECRET: ${SESSION_SECRET:-YJE6swc9LcPFU2klTuxM++mkUkSZdfIRWKbr0pfwLih75u3p/Yd+OyAYa8ymvYYi}
      AUTH_DATA_DIR: ${AUTH_DATA_DIR:-/app/data}
      LOCAL_AUTH_ENABLED: ${LOCAL_AUTH_ENABLED:-1}
      AUTH_EMAIL_FROM: ${AUTH_EMAIL_FROM:-LumiTales <lumibooks888@gmail.com>}
      RESEND_API_KEY: ${RESEND_API_KEY:-}
      RESEND_API_URL: ${RESEND_API_URL:-https://api.resend.com/emails}
      SMTP_HOST: ${SMTP_HOST:-smtp.gmail.com}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USER: ${SMTP_USER:-lumibooks888@gmail.com}
      SMTP_PASS: ${SMTP_PASS:-ocwbqiaafnnksudc}
      SMTP_SECURE: ${SMTP_SECURE:-1}
      SMTP_STARTTLS: ${SMTP_STARTTLS:-0}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      APPLE_CLIENT_ID: ${APPLE_CLIENT_ID:-}
      APPLE_TEAM_ID: ${APPLE_TEAM_ID:-}
      APPLE_KEY_ID: ${APPLE_KEY_ID:-}
      APPLE_PRIVATE_KEY: ${APPLE_PRIVATE_KEY:-}
